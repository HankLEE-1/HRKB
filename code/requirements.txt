# ── Core deep learning ───────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── Transformers & model backends ────────────────────────────────────────
# 4.45+ required for Qwen2_5_VLForConditionalGeneration
transformers>=4.45.0
accelerate>=0.26.0          # device_map="auto"
sentencepiece>=0.1.99       # XLM-RoBERTa tokenizer
protobuf>=3.20.0

# ── Vision & image processing ────────────────────────────────────────────
Pillow>=9.5.0
qwen-vl-utils>=0.0.8        # Qwen2.5-VL vision input processing

# ── Scientific computing ─────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0         # K-Means, silhouette

# ── Utilities ────────────────────────────────────────────────────────────
tqdm>=4.65.0
requests>=2.28.0

# ── Optional: Flash Attention 2 (recommended for Stage 2) ────────────────
# Install separately after torch: pip install flash-attn --no-build-isolation
# flash-attn>=2.3.0
